org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Default published library version for local builds. The release workflow
# overrides this via -PVERSION_NAME=<git tag, without the leading "v">.
VERSION_NAME=1.0.0
